const NotFound = () => {
  return (
    <div>NotFound</div>
  )
}
export default NotFound


// Group routes also serve as a way to organize your files without affecting the URL structure.
// Group routes can have nested layouts, templates, and error components.
// If you are having HTML and BODY mismatch issues, ensure that your group routes are not causing the problem.